---
- name: Take an explicit consistent NAS backup
  hosts: rust_servers
  become: true
  gather_facts: false
  tasks:
    - name: Run configured backup service
      ansible.builtin.systemd_service:
        name: rust-backup.service
        state: started
