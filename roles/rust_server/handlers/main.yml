---
- name: Reload Rust units
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Rust
  ansible.builtin.command:
    argv: [python3, /usr/local/libexec/pi5-rust/service_state.py, restart]
  changed_when: true
  when: rust_start | bool
